services:
  jupyter:
    build: .
    ports:
      - "8889:8888"
    volumes:
      - ./.notebooks_data:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    command: start-notebook.sh --NotebookApp.token=''